форма добавления поста - приделать редактор, фильтр скрипт тэгов, далее опробовать формсет с jquery
бесконечная прокрутка средствами django/ajax
Как можно больше страниц через ajax

тэги
...
социальная аутентификация


формсеты и динамическое добавление форм
превью и полная картинка для изображений(лайтбокс)


 var jq = document.createElement('script');
jq.src = "http://127.0.0.1:8000/static/js/jquery-latest.js";
document.getElementsByTagName('head')[0].appendChild(jq);
// ... give time for script to load, then type (or see below for non wait option)
jQuery.noConflict();
 
 jQuery.each( $('*'), function() { 
     if (  jQuery ( this ).width() >  jQuery ( 'body' ).width()) {
        console.log( "Wide Element: " ,  jQuery ( this ),  "Width: " ,  jQuery ( this ).width());
    } 
});


Загрузка категории через ajax
$.ajax({
          cache: false,
          type:"GET",
          url:"test2",  
          success:function(data){
             data2 = ('<div class="content">' + data + '"</div>"');
               $(data2).replaceAll('.content');
          }
     });